export function MarketplaceRating({average,count}:{average:number;count:number}){if(!count)return <span className="text-[11px] text-[#6b8bab]">New seller</span>;const rounded=Math.round(Number(average));return <span className="inline-flex items-center gap-1 text-[11px] font-bold text-[#9db4d1]" aria-label={`${Number(average).toFixed(1)} out of 5 from ${count} reviews`}><span className="tracking-[-.1em] text-[#d4af37]">{"★★★★★".slice(0,rounded)}</span><span>{Number(average).toFixed(1)} ({count})</span></span>}
